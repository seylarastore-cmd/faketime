<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Hint shown in the LSPosed manager. Scoping is done per-app by the user:
      LSPosed only injects this module into processes the user checks in
      Modules -> FakeTime -> scope. This array just preselects System
      Framework in the manager; it does NOT auto-scope every app.
    -->
    <array name="xposed_scope">
        <item>android</item>
    </array>
</resources>
